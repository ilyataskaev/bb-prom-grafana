# Blackbox prober exporter

## Usage

To start:

```bash
# In the foreground
docker-compose up --build

# In detached mode
docker-compose up --build --detach

# In detached mode
docker-compose kill
```
